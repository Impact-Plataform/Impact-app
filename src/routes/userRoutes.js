const { Router } = require('express')
const router = Router()
// const userController = require('../controllers/validateUser')

// router.get('/validate', validateUser)

// router.post('/login', login)

module.export = router
